---
type: synthesis
status: template
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags:
  - synthesis
  - context
---

# Core Argument Map

本页保存长期对话后的短上下文快照，用于在上下文变长时快速恢复项目状态。

## Current Project Goal

- 使用 ResearchWiki 将论文转化为可维护的 Markdown 知识库，用于阅读、综合、gap 分析、研究定位和综述写作。

## Confirmed Decisions

- `AGENTS.md` 作为轻量总控入口。
- `agents/` 保存任务型规则。
- `templates/` 保存页面输出模板。
- `memory/` 保存长期约束和配置。
- `wiki/` 保存结构化知识页面。
- `synthesis/` 保存跨论文综合成果。
- `raw/` 保存原始资料，但开源时默认不上传真实 PDF 或附件。

## Deprecated or Rejected Options

- 待填写

## Current Paper Corpus

- 待导入论文后更新

## Next Tasks

- 填写 `memory/project_profile.md`。
- 配置 `memory/tag_taxonomy.md` 和 `memory/term_aliases.md`。
- 导入第一篇论文。
- 运行 `agents/lint_agent.md` 做健康检查。

## Open Questions

- 待填写

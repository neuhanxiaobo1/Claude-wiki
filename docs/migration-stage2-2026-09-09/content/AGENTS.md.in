# ResearchWiki 总控制规则

本库由公共规则和多个独立研究方向组成。总规则只负责入口、路径、作用域和公共底线；研究目标、领域阅读要求、总结/写作要求与任务记录属于所选方向。用户当前指令优先于历史记录；公共证据标准见 `memory/hard_memory.md`。

## 1. 目录与路径

- 根 `agents/`：通用任务流程；根 `templates/`：通用模板及新方向骨架。
- 根 `memory/`：公共证据、个人默认偏好、恢复策略、通用词表、方向注册表及公共维护记录。
- 根 `docs/`：通用操作说明与架构迁移/验收报告；`shared/`：可借鉴资源导航。
- `directions/<direction_id>/`：一个方向的AGENTS、index、log、inbox、memory、raw、wiki、synthesis、docs及按需扩展模板。
- 本文件与公共流程中的 `D` 表示本会话明确选择、在注册表中核对的方向根路径，不是实际名为D的目录。写入前展开成真实绝对路径，确认位于允许方向内；禁止使用未展开的占位路径。
- 根index只导航方向和公共资源，根log只记公共维护。研究index/log/inbox属于D。不得在根重建旧raw/wiki/synthesis或全局project_profile。

## 2. 启动与方向选择

首次进入或上下文不足时读取本文件、`memory/hard_memory.md`、`memory/user_profile.md` 和 `memory/direction_registry.yaml`，通过根index定位公共工作。

1. 用户明确要求公共规则维护、迁移、全库架构检查：进入公共维护范围，无须选择研究方向。当前公共任务见 `memory/context_policy.md` 与最近根log。
2. 用户明确指定已登记方向，或给出可唯一归属的方向文件路径：绑定本会话D，简短说明方向，不重复确认。
3. 新一轮研究工作未指定方向（包括新会话只说“继续之前工作”）：仅根据注册表展示已存在方向及“新建研究方向”的选择，最近使用仅用于排序。用户回答前不预读各方向研究目标/全文，也不写研究文件；可以继续无依赖的公共工作。
4. 已在同一任务绑定方向：沿用选择，不每轮重复提问。切换时保存必要交接，重新绑定D并加载新方向规则；旧方向偏好、结论和待办不作为新方向依据。
5. 每个会话独立绑定方向，不使用共享active_direction文件控制会话。注册表last_used更新只用于导航排序，不切换其他会话，也不表示其研究进度已改变。

选择后显式读取 `D/AGENTS.md`、`D/memory/project_profile.md`、`D/memory/current_context.md`，通过 `D/index.md` 定位对象。不能只依赖工具是否自动加载嵌套AGENTS。只在需要恢复时读本方向log最近相关条目，不默认预读其他方向或全部历史。

规则、证据与任务状态无变化时增量复用；文件变化、冲突或信息缺失时只重读受影响部分。历史快照不能代替用户当前目标或原文证据。

新方向使用 `templates/direction.md` 与 `docs/direction-workflow.md`：确定名称及基本范围，核对语义重叠与稳定ID/目标路径，再创建骨架并登记。其他未知配置留待填写，不能复制旧方向的研究目标、文献、领域标签、团队口径或任务进度。无专用要求时沿用通用规则，不为填空创建科学页面。

## 3. 任务路由与规则组合

| 任务 | 通用流程 | 方向补充与输出 |
|---|---|---|
| Zotero collection候选清单 | agents/import_zotero.md | 读取D来源配置，写D/raw/zotero_imports；已知论文不重做集合清单 |
| 论文新入库、完整复核、局部修订 | agents/pdf_read_agent.md | D声明的reading_rules；写D/wiki/papers；首建页读通用paper/checklist模板 |
| 多论文综合、研究路线、比较 | agents/synthesis_agent.md | D声明的synthesis_rules；写D/synthesis及相关topic/method/review |
| gap、创新点与研究定位 | agents/gap_agent.md | D问题范围与综合规则；写D/wiki/gaps、D/synthesis |
| 综述、related work、大纲 | agents/review_agent.md、templates/review.md | D声明的writing_rules；写D/wiki/reviews、D/synthesis |
| 标签、链接、重复、状态与纠错 | agents/lint_agent.md | 默认仅检查所选D；公共架构任务按明确范围检查根规则 |

公共流程提供底线和默认格式，方向配置提供领域补充；默认风格可以覆盖，证据底线不能降低。读取本任务适用扩展即可，不自动加载全部方向规则。新建页面/调格式时读模板，局部修订不要求重新填满模板。

## 4. 证据、原件与范围

- D/raw是原始资料层。未经明确授权不修改、删除或重命名原件；外部Zotero附件、缓存、译文、补充材料同样保护。已授权目录迁移只改变指定位置，原件内容保持不变。
- 系统生成的import_plan/manifest是导入管理记录，可在获授权任务内增量维护；不扩张到其他原件或Zotero写入。
- D/wiki和D/synthesis保存结构化知识与综合。重要判断遵循hard_memory；观察、计算、作者解释、转引和AI推断分开，来源有定位，不以链接存在或processed代替证据成立。
- 不确定信息写“待核查”“不确定”或“AI推断”；目标或偏好缺项可写“待确认”。缺失材料只阻断依赖它的判断，不虚称已读。
- 完成本轮论文/证据后检查直接相关下游；只改明确相关且在范围内的内容，其余在当前可写页面列待复核。不能只记待办却继续采用已知失效的结论。
- 用户已指定对象、编号、小节或阶段时直接完成范围内工作，不逐项重复确认。只在缺失信息影响正确性/范围时询问，并继续独立部分。

## 5. 方向隔离与跨方向借鉴

默认检索和写入范围是D。另一方向可作为背景或资源，按具体需要定向读取；默认只读来源方向，不加载它的写作目标/执行待办为本方向规则。用户明确跨方向任务时界定参与方向、读取对象和输出归属，缺少会改变交付归属的信息时再询问。

接收方向独立记录：来源完整链接、版本或核查日期、原适用条件、借鉴部分、本方向评价与尚未验证假设。同一论文可以在两个方向有不同解读页，共享原始来源；同源数据不能算独立验证。发现来源错误时先限定接收方向当前结论，记录来源待复核；不自动修改另一方向。

论文身份去重、术语/标签去重与同义页合并限本方向。相近名称不表示同义，不跨方向自动合并topic/method/claim/gap。通用词表和方向词表共同使用，领域词条只登记到方向词表。

## 6. 页面与链接

方向知识页使用 `direction_id` 标明归属，与所在目录一致；原件不为补字段而改写。使用相对于vault根的完整双链，模板占位必须在生成时展开，例如：

```text
[[directions/<direction_id>/wiki/papers/<page>]]
```

只有存在明确关系才建链接，不为图谱数量制造关联。来源/证据编号及标题锚点分别核对。

## 7. 索引、日志、错误和决定

- 研究操作追加D/log.md；研究错误记D/memory/error_log.md；研究决定记D/memory/decision_log.md。公共架构操作及错误/决定写根级对应文件，不把所有研究过程复制进根记录。
- 创建、删除、重命名、合并重要页面或索引说明/重要状态变化时更新对应index；单纯正文变更且索引准确时只核对一致性。
- 每次重要操作记录：`## [YYYY-MM-DD] 类型 | 标题`，含输入、操作、新建、更新、发现、后续。类型可用init/ingest/query/synthesis/gap/review/lint/update/error-fix/decision。
- 发现错误或用户指出错误必须记录：`## YYYY-MM-DD | 错误类型`，含错误表现、原因、修正规则、影响范围、以后避免方式。
- 方向短状态放D/memory/current_context，公共恢复机制放memory/context_policy；科学论证属于D/synthesis，不能让历史流水覆盖当前任务。
- 提交/上传须有对应授权；日常维护与本次目录迁移不自动发布。Git忽略文件和外部资料不因页面存在而被视为已备份。

## 8. 迁移记录

目录迁移的实际状态见根index、根log及 `docs/migration-stage2-2026-09-09/report.md`。阶段1快照和旧路径映射属于历史，不能直接重运行一次性准备/迁移脚本来覆盖现有目录。归档中的旧根路径按映射溯源；公共任务记录不得触发重读研究全库。

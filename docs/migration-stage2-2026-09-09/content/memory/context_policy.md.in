# 公共上下文与恢复策略

本页保存恢复机制和公共维护短状态。研究方向进度在D/memory/current_context.md；研究目标在D/memory/project_profile.md。D只由本会话明确选择，不从共享最近使用时间推断。

## 增量恢复

- 首次进入遵循根AGENTS，先区分公共维护与方向研究；未指定研究方向时先选择。
- 同一任务复用已读未变规则和证据，仅加载新增输入、受影响文件与本任务需要的扩展。
- 跨阶段/压缩后，读所属范围的短状态、对应计划与最近相关日志；能确定对象、授权范围、完成边界和下一步后停止回溯。
- 规则变化、实际文件与快照冲突时重读相关部分；用户当前指令优先，不凭文件日期判定科学结论正确。
- 同一对话换方向时保存原方向必要交接，绑定新D并重新加载方向配置；其他方向的规则、待办与论证不自动沿用。

## 压缩与记录

跨多个阶段、出现规则/目录结构变更、状态冲突或需要反复翻查旧对话时，可更新短状态。只保留对象、任务类型、范围、完成条件、下一步、关键约束、未决项和必要入口，不复制详细流水；科学证据仍回原文。

不保留与任务无关信息，不把被放弃建议或AI候选定位写成用户目标。压缩本身不改变证据状态、不删除历史，也不触发额外整理任务。普通只读问答无需机械写盘；重要阶段完成或范围改变时维护短交接。

| 内容 | 位置 |
|---|---|
| 用户公共默认偏好 | memory/user_profile.md |
| 公共规则、错误、架构决定 | memory/hard_memory.md、memory/error_log.md、memory/decision_log.md |
| 公共工作流水与阶段报告 | 根log.md、docs/ |
| 研究目标、来源与适用规则 | D/memory/project_profile.md、D/AGENTS.md |
| 本方向当前任务和恢复入口 | D/memory/current_context.md |
| 本方向流水、错误与研究决定 | D/log.md、D/memory/error_log.md、D/memory/decision_log.md |
| 科学主张与证据关系 | D/wiki、D/synthesis |

## 当前公共任务

- 2026-09-09：用户授权多方向改造阶段2，当前批次包含规则拆分、既有方向迁移、路径修复和方向选择入口。
- 实际验收状态以 [[docs/migration-stage2-2026-09-09/report]] 与根log为准；进入下一阶段前核对报告，不重复执行已迁移的文件操作。
- 下一步：阶段3隔离验收；第2个实际方向名称与基本范围留阶段4收集，真实论文试运行留阶段5。
- 既有研究交接已移入方向目录；公共启动不预读其研究问题和全部历史。

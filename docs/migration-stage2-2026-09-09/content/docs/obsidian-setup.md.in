# Obsidian 设置

将整个ResearchWiki目录作为一个vault打开，多个研究方向分别位于directions/<direction_id>。本机workspace属于私有状态，不作为研究方向选择或任务授权依据。

- 新知识页使用相对vault根的完整双链，避免不同方向同名topic/method跳转歧义；app.json的newLinkFormat已设置为absolute。
- 当前图谱过滤为 `path:directions/ceramic-corrosion/wiki/`；查看其他方向时替换方向ID，查看全体方向可用 `path:directions/`。图谱过滤只影响显示，不能作为读取/写入隔离。
- app/graph/workspace是全vault共用配置，两个会话的方向绑定由各自对话保存，不靠修改共享设置切换。
- 迁移时保留了workspace中历史临时文件条目并更新其路径前缀；历史不存在的临时文件不重建。实际Obsidian窗口显示效果需打开应用查看，文本配置校验不等于桌面实测。
- 本地迁移ZIP位于.migration-backups，Git忽略；不要解压回vault制造重复可写知识页，回退按迁移清单执行。

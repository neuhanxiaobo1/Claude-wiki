# 优化阶段C：只读检查、恢复与备份范围

2026-09-10。用户要求继续阶段C，本阶段已完成工具、规程与声明范围的验收。未执行真实论文、独立会话、Obsidian桌面或备份恢复演练。

## 本轮产物

- [只读检查工具](../../scripts/wiki_check.py)、[34项回归测试](../../scripts/test_wiki_check.py)与[scripts/requirements.txt](../../scripts/requirements.txt)。使用现有Python/PyYAML，无新安装、后台服务或自动修复。
- [使用说明](../structure-check.md)：明确公共、指定方向、全部方向三个入口，以及语法、退出码、未检查内容。
- [恢复与备份范围](../recovery-and-backup.md)：部分骨架、批次中断、paper/清单不同步的续做；写前版本核对、冲突处理及分层恢复范围。
- 根规则、lint、快速入口和方向创建说明已链接新能力；不重跑一次性迁移脚本。

## 实际工具验证

`python -B scripts/test_wiki_check.py`共34项：33通过，1跳过。通过项覆盖注册表结构/重复/路径越界、缺公共入口、未登记的部分方向、YAML语法及重复键、方向/type错配、状态/来源缺失、文件/标题缺失、旧长标题、显示别名、块ID、短名歧义、代码/模板豁免、真实占位未展开、范围隔离、raw不解析、相对路径、同名目录优先级、文件不变及强制选择范围。

跳过的是当前账户实际创建符号链接的测试；禁止reparse路径的判断分支已用模拟文件属性验证，但不能据此宣称真实Windows junction/符号链接场景已全面实测。测试只在系统临时目录建立虚构资料。

实际库扫描结果保存为JSON快照：

| 范围 | 选中文件数 | 结构错误 | 提示/未检查 | 结果 |
|---|---|---|---|---|
| 公共 | 34 | 0 | 18 | [public.json](public.json) |
| 陶瓷腐蚀 | 40 | 0 | 2 | [ceramic-corrosion.json](ceramic-corrosion.json) |
| 电子封装 | 9 | 0 | 1 | [electronic-packaging.json](electronic-packaging.json) |
| 公共及全部方向 | 83 | 0 | 23 | [all-directions.json](all-directions.json) |

这些是本阶段扫描时点结果，之后文件变化需按范围重查；不能把83个文件解释为83篇论文。科学证据核查始终为false。

## 遗留提示的解释

- README的18个Markdown目录锚点：本版核对本地文件目标，但不实现渲染器slug/Markdown锚点验证，全部列未检查；不是18个已确认断链。
- 全范围有5处短名多候选：陶瓷index中的index与AGENTS、电子封装index中的index、根index中的AGENTS与inbox。它们涉及根与方向同名入口，本工具不模拟Obsidian全部候选优先级，保守报告待人工核对；未据此批量改名或修改研究方向入口。
- 方向单独扫描的候选集合小于全范围，提示数不直接相加；范围外同名不自动扫描。

本轮未发现已确定的结构错误。下一阶段可在Obsidian中验证这5处实际跳转意图，再决定是否需要明确化链接；不能把候选歧义或未实现语法当作科学错误。

## 发现并修正的工具问题

初版检查文档相对路径时，在正确相对目标已存在后仍尝试根目录回退，产生越界提示；另把同名目录与无扩展名笔记并列，导致误报。已改为有效相对Markdown目标优先、对应笔记优先于同名目录，并加入两个回归案例。最终JSON已重新生成，保留的23项不含上述误报。

## 文件保护与备份盘点

本阶段前基线在`.migration-backups/optimization-stage-c-20260910T160144/`。实际核对115个方向文件、注册表及hard_memory共117个文件哈希一致，方向文件成员不变；原件、科学页面、方向配置和研究任务状态未改。

只读盘点本机raw非占位文件：陶瓷18个，共2,313,220字节，均未Git跟踪；其中12个管理记录文件名候选未据此认定内容身份。电子封装为0。未读取或备份外部Zotero/MinerU原件。Git版本、未提交改动、raw管理记录、原件、外部附件和本机快照的覆盖分别列明。

独立备份目的地/频率尚未指定，未建立新同步或计划任务；真实恢复演练留阶段D。当前同盘快照不代表已具备异地恢复能力。

## 取舍及下一步

工具是一份显式范围的只读诊断程序，无自动修复、全库默认扫描、后台锁或外部连接。语法覆盖有限，范围外锚点、语义去重、标签意义、科学论证及来源可用性仍需相应流程判断。写前哈希核对属于恢复规程，不能消除其他程序的并发竞态。

阶段C完成O08的独立检查入口、O09的恢复约定和O11的备份范围盘点；不把文档约定称为真实运行时隔离或备份完成。下一步阶段D需明确电子封装首篇论文及可访问来源，结合局部修订、定向借鉴、真实会话/桌面与恢复样例逐项验收。缺输入只阻断对应案例。

本轮未提交上传；已发布calude_wiki_2.0标签保持不变。

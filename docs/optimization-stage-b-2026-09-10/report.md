# 优化阶段B：模板与收尾精简

2026-09-10。用户要求继续下一步，沿用阶段A后的阶段B范围。本阶段完成规则/模板修改及本报告声明的验证；真实论文、桌面和会话实测仍留阶段D。

## 实施结果

论文仍按原阅读流程完整处理必要材料，输出用一份paper承载结果。关键证据E#记录原始结果，Conclusions for Reuse内的Finding评价结论、支持程度、边界、作者解释和本次判断；写作用途需要时附在该Finding，不重复转录结论。

阅读检查单改为一次适用项核对入口，不填写或另存第二份卡片。用户明确要求核对报告时可以保存本次结果，引用已有证据即可。旧论文不批量换模板；详细精读、推导和方向要求继续按任务保留。此次合并不把用户的篇幅或精读偏好设为“简略”。

根AGENTS第7节统一记录触发条件：主log记重要交付，index导航，短状态恢复当前任务，inbox存候选/未分配事项，错误/决定各记其实际变化。已知错误与关键交接必须保存；普通只读问答无错误、状态变化或保存请求时不机械写盘。

## 栏目合并与信息保留

| 原位置 | 本轮处理 | 保留要求 |
|---|---|---|
| Metadata正文重复元数据 | 正文补来源映射和限制，已在frontmatter写过的信息不重抄 | frontmatter字段/默认值完全保留；实际来源、身份、版本仍可追溯 |
| Takeaway | 不再强制独立栏目，需要总览时引用Finding/E# | 限定措辞、条件和不确定性仍在结论旁 |
| Findings and Interpretation、Conclusions for Reuse | 合并为Conclusions for Reuse中的Finding | 判断、E#、支持理由、适用条件、作者解释、本次评价及保留/收窄/撤回决定均保留 |
| Notes for Review Writing | 按任务在相关Finding补写 | 用途、允许措辞与使用前缺失证据仍可记录，不强制每篇作写作规划 |
| Relation to Existing Wiki、Linked Pages | 关系按实际论述链接或用已有frontmatter记录；需要比较分析时展开 | 真实关系、可比条件和方向借鉴说明不省略，不为图谱强制建页 |
| Maintenance与长检查单 | 以简短核对表检查，不复制勾选记录 | 具体错误/未决必须落盘，重要操作及变化按根规则维护 |

保留Study Design、Key Evidence、Conclusions for Reuse、Downstream Review等下游入口。synthesis和lint已明确新旧布局兼容；电子封装规则要求的Study Design、Key Evidence及相应Finding均有落点，陶瓷专属作者信息仍按方向规则扩展。

## 副作用复查

- 合并输出不减少阅读：pdf_read第2–5节及第8节原文保持一致，hard_memory整个文件与阶段B前逐字节一致。
- 保留真实引用：新模板仍有稳定E1标题，旧方向页面标题/正文/状态均未改。
- 避免把不填检查单当作不检查：短清单逐项指向实际记录落点，失败项继续限定相关判断。
- 避免删去“无下游”时丢掉源证据未决：模板明确本条E#/Finding仍记录源证据自身的缺失影响。
- 避免少记日志导致无法恢复：错误及交接为必须记录的触发；已选任务在短状态维护，不在inbox复制另一套进度。
- 避免把简化推广成全库重写：本轮只修改公共模板/流程/记录规则与公共维护文档，方向文件无变化。

## 场景逐案审阅

以下是规则组合审阅结果，不是已经运行过的用户对话或论文试验。

| 情境 | 按修改后规则应执行的行为 |
|---|---|
| 普通只读问答，无新错误/状态 | 回答即可，不强制更新研究文件 |
| 只读检查发现实际错误 | 记录错误、限制受影响判断；不能援引只读例外省略 |
| 用户明确要求保存检查报告 | 保存报告，重要交付记主log；按新报告是否需要导航判断index |
| 新论文入库 | 一份paper；完成核对；记重要操作，补导航；当前任务确实变化时更新短状态 |
| 已有论文局部修订 | 原栏目内修订指定判断和直接影响，不重填整卡、不自动升级整页 |
| 重要阶段中断 | 保存已完成边界、阻塞/下一步和必要主日志，不把未完成内容标完成 |
| 已选定inbox候选 | 在inbox标记已选/移交并链接短状态，后续执行进度只在短状态维护 |

## 实际检查与文本量

修改前主要目标文件快照与全部方向哈希位于`.migration-backups/optimization-stage-b-20260910T154812/`。这是本轮检查基线，不是外部原件完整备份；恢复时只对具体修改段落比较，不整体覆盖前后轮的修改。

执行[verify.py](verify.py)：

```powershell
python docs/optimization-stage-b-2026-09-10/verify.py --baseline .migration-backups/optimization-stage-b-20260910T154812
```

21项检查通过：方向文件成员、115个方向文件及注册表哈希不变；hard_memory不变；根AGENTS除第7节外的编号章节不变；pdf_read第2–5、8节不变；paper元数据字段和默认值一致；核心下游入口、Finding归属、E1锚点保留；检查单不再有第二套待填E#/Claim字段，重复独立输出栏目已合并。

| 文件 | 原行数→现行数 | 原字符数→现字符数 |
|---|---|---|
| templates/paper.md | 165→115 | 4,095→3,451 |
| templates/pdf_ingestion_template.md | 115→28 | 3,878→1,080 |
| agents/pdf_read_agent.md | 142→144 | 3,801→4,049 |
| 合计 | 422→287 | 11,774→8,580 |

统一换行后的字符量减少3,194，约27.1%；仅指这三份文档，不是全库体积、token消耗或耗时减少。流程中的兼容提醒略有增加，检查单去除重复填表贡献了主要缩减。Git差异检查通过；未测实际论文准确率、交互次数或运行耗时。

## 下一步

阶段B完成，O05完成本轮模板合并，O07完成记录职责与触发约定；执行效果仍须真实案例核验。接下来阶段C：补独立的只读结构检查入口、明确中断/并行写入恢复及备份范围，不重跑迁移脚本。需要外部备份目的地或真实论文时再处理依赖该输入的部分。

本轮未修改任何方向研究文件、注册表或原件，未提交上传，calude_wiki_2.0标签保持不动。

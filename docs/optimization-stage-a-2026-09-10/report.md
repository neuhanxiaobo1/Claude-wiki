# 优化阶段A：方案复查、规则统一与兼容性验收

日期：2026-09-10。阶段A已完成本报告声明范围。用户授权：进一步检查方案及副作用，确认后实施第一步。

## 复查结论与收紧边界

方案总体可实施，但不宜把删除重复文字等同于流程优化。先统一生成规则和兼容约定，保留必要入口提醒与完整证据要求；原定阶段B的论文模板精简继续保留为下一阶段，尚未替用户决定日常精读详略。

| 潜在副作用 | 本轮处理 | 剩余边界 |
|---|---|---|
| 删除入口后直接调用agent会漏读根规则 | 六份agent保留根启动、方向加载和hard_memory入口；根AGENTS明确直接点名agent也走启动 | 文本规程不能强制不遵循规则的外部程序；真实新会话仍待阶段D |
| 只把#E1移进双链，旧标题仍不匹配 | 新记录使用独立标题E1；旧页引用真实完整标题，不批量改名 | 本轮只验生成示例与兼容样例，未检查既有全库锚点 |
| 插入/重排证据导致E#指向另一条结果 | 已分配编号稳定；不同来源/对象/指标新分配ID；拆分或撤回保留旧ID处置 | 直接下游按任务范围检查，其他方向默认只读 |
| 统一状态时把局部已核变成整页checked | 定义集中到hard_memory；综合表分开页面状态与本条核查范围；旧字段不批量改 | 已有checked声明不等于本轮重新验证 |
| 新字段增加机械填表 | 核查范围用实际材料与未决的文字表达，不增加必填枚举或新状态系统 | 详细模板瘦身留阶段B，不在本轮压缩科学记录 |
| 不重复确认变成跳过来源消歧或取消批次偏好 | 保留陶瓷默认上限3，仅澄清已授权对象无需再问；来源身份仍需核对 | 原有研究目标、筛选偏好与来源配置不变 |
| 本轮覆盖上轮尚未提交的评估记录 | 修改前对本机当前文件保存快照；增量补写计划/记录 | 不以Git版本整体回退覆盖用户或前轮修改 |
| 公共规则集中后启动文本反而增长 | 如实测量字符数；新增规则是兼容和职责定义，保留增量复用 | 本轮不宣称总读取量、token或耗时降低 |

链接格式核对了[Obsidian官方内部链接说明](https://obsidian.md/help/links)（访问：2026-09-10）：标题定位使用双链内部的#加标题文本。选择稳定标题作为新记录默认，不引入必需的块引用；原有有效定位继续兼容。官方语法核对不等于桌面点击实测。

## 已实施修改

- 根AGENTS第3节增加规则出处与冲突处理，第6节确定稳定证据ID、链接和修订兼容约定。
- hard_memory统一页面review_status含义，说明与单条证据核查范围、支持程度、处理状态的区别；原Evidence Rules段落完整保留。
- 六份agent精简共同入口，保留方向绑定、来源只读和证据底线提醒；pdf_read引用状态定义，lint增加旧标题/撤回证据检查说明。
- synthesis/review流程和claim/gap/method/review模板共六处证据示例改为双链内真实标题定位；paper新建记录标题改为E1，描述另行记录，其余栏目保留。
- 综合矩阵拆开Source page review_status和Evidence verification scope；claim/method表格明确单条核查范围。
- README纠正方向词表、公共进度与历史数量说明；QUICKSTART及方向规程增加自然语言入口和明确授权的执行说明。
- 陶瓷profile只改默认确认措辞和更新日期，其他方向配置与全部科学页面保留原内容。

对应方案：O01/O02/O03/O12已完成本阶段修正；O06完成稳定ID和兼容约定，自动反向引用仍留后续；O04完成六份流程入口去重，全面模板/收尾精简留阶段B。

## 实际验证

修改前快照：`.migration-backups/optimization-stage-a-20260910T150456/`，内含修改前公共文件、陶瓷profile及194个本机文件的哈希清单；方向其他文件仅建立哈希基线，没有以该快照冒充外部原件备份。

执行[verify.py](verify.py)：

```powershell
python docs/optimization-stage-a-2026-09-10/verify.py --baseline .migration-backups/optimization-stage-a-20260910T150456
```

21项针对性检查通过：

- 两方向文件成员保持一致；除明确修改的陶瓷profile外，114个方向文件逐字节哈希不变；注册表不变。
- 六处示例在替换方向路径后均能定位到新paper模板的实际E1标题；活动agent/template中无双链外附E#旧示例。
- 反例检查能区分双链外后缀、缺失E9、错误方向路径，以及旧长标题被错误缩成E1；旧完整标题仍可定位。
- 六份知识页模板review_status默认仍为draft；综合示例分开页面状态/证据范围，未保留错误的页面partial示例。
- 原hard_memory的Evidence Rules段落逐段保留；六份agent均保留根规则及证据入口。

另已人工复查以下规则组合：公共维护不选研究方向；新研究未指定方向时等待选择；同一任务复用；明确批次不逐篇重问；局部修订不提升整页；跨方向来源默认只读；旧标题保留；撤回记录可追溯但不能作为支持。它们是规则审阅，未冒充真实对话验收。

Git差异检查未发现新增空白格式错误；陶瓷profile差异仅为上述两处。科学内容、原件、方向任务状态、注册表、历史迁移脚本、Git标签及远程仓库未改变。

字符量（统一文本换行计数）：六份agent合计21,092→20,268，减少824字符；首次启动四文件合计8,056→9,304，增加1,248字符。规则集中和补齐定义带来此项取舍；不能仅据agent变短宣称整体提速，实际加载量应在后续任务测量。

## 验证限制与恢复

脚本只验证完整路径双链和精确ATX标题样例，以及文件一致性；不支持全部Obsidian链接语法、块引用或重复标题消歧，不执行桌面、不验证科学支持程度、不写研究文件。该脚本是本阶段回归检查，不代替阶段C通用只读检查器。

本轮未发现阻碍阶段A启用的问题，结论限本报告已检查范围。若后续发现入口遗漏或引用兼容问题，先按快照逐文件比较，仅恢复相关段落，并保留此后新增的用户修改；不批量覆盖目录、不重跑历史迁移、不移动calude_wiki_2.0标签。

下一步阶段B：在完整证据要求下精简模板和维护收尾。详略偏好仍未确定，可先做不影响科学覆盖的重复栏目归并设计；真实入库、跨会话、桌面与恢复演练继续按后续阶段验证。本轮未提交或上传。

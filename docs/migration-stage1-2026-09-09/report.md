# 多方向迁移阶段1：准备与验收

日期：2026-09-09。阶段1完成，阶段2尚未执行。当前目录、研究流程与论文页面保持单方向原结构；下一步是协调启用方向规则和迁移当前方向。

## 交付入口

- [逐文件归属及旧→新路径映射](inventory.json)：每个源文件都有目标、操作、附加目标、说明、大小、SHA-256、Git跟踪/忽略状态。
- [混合文件逐段拆分表](section-routing.md)：规定一对多拆分、多个来源合并、历史保留与受保护笔记处理，补充机器清单。
- [备份及盘点摘要](summary.json)：最终快照时间、Git基线、备份实际路径和ZIP哈希；备份路径以此文件为准。
- [raw文件哈希](raw-hashes.json)：包含被Git忽略的清单、元数据及原始笔记。
- [链接基线](links-baseline.json)：记录源文件、行号、目标、解析状态、示例语境、锚点及尾随E#。
- [路径引用位置](path-mentions.json)：补充记录正文、JSON、CSV和配置中的路径所在行，供阶段2定向修订；不等于全部路径已验证。
- [生成脚本](prepare.py)：只做盘点、备份和阶段产物生成，不移动研究文件。

## 实际盘点

最终源快照包括112个文件，约3.14 MB；92个被Git跟踪，19个被忽略，1个未跟踪（阶段0方案）。含隐藏配置与 `.gitkeep`。排除 `.git/`、`.migration-backups/` 和本阶段报告目录，外部Zotero/MinerU资料不在备份范围。

| 内容 | 数量与处理 |
|---|---|
| wiki论文卡片 | 10篇 |
| claim / gap / topic / review | 8 / 5 / 2 / 1页 |
| synthesis | 6页，包括组外作者登记表 |
| raw | 22个文件，其中4个.gitkeep、1篇原始笔记、17份导入/筛选记录 |
| raw中的PDF | 0个；已有论文源页引用工作区外文件，不能宣称这些PDF已备份 |

当前方向目标固定为 `directions/ceramic-corrosion/`。全部主目标检查无重名冲突，也无已存在目标；一对多拆分及reading_rules合并另按逐段拆分表执行。

## 备份核验

已生成 `.migration-backups/` 下的带时间戳ZIP，并逐文件读回校验：112/112项内容SHA-256与源快照相符，ZIP完整性检查通过，快照期间源文件无漂移。备份文件名和压缩包SHA-256见summary。

该目录已加入 `.gitignore`，实际check-ignore验证生效。保留全部本地准备快照，以summary引用的最终快照作为下一阶段入口；没有提交或推送。备份是同盘的迁移回退副本，不包括Git对象库、外部PDF/解析缓存，也不是异地灾备。阶段报告目录自身是新增可再生成的准备产物，未纳入源快照。

阶段2回退时先核对ZIP哈希、成员和逐文件哈希，按inventory及实际执行记录只恢复本轮迁移影响的路径；不能整库覆盖或删除后来新增的用户资料。

## 链接基线与限制

快照共识别1095处链接语法：1042处目标文件可解析，18处同文件链接，18处外部URI未访问，17处未解析。未发现歧义短链。

17处未解析目标均经逐项检查属于示例：AGENTS六处、阅读/综合任务示例表三处、方案中未来路径一处、术语示例一处、模板六处。不是17个实际知识页断链；不创建这些占位页面。agents表格中的三项虽由脚本标为body，人工确认为Paper/Claim示例。

本次只建立迁移对照基线，不是完整链接或证据审计：

- 已记录标题/块锚点及尾随 `#E1`，未验证全部锚点是否有效；链接目标存在不等于证据成立。
- 支持常见双链、图片嵌入和行内Markdown链接；复杂嵌套括号、引用式Markdown链接、HTML、Dataview和Obsidian实际跳转未覆盖。
- 外部本机PDF/MinerU路径保留在路径引用清单，未访问或计算哈希；本轮不重查科学原文。
- 原始Si3N4笔记内容保护与内部旧链接的处理按逐段表执行；它不纳入活跃派生页链接自动改写。

## 阶段2接续顺序

1. 重新核对当前文件与本快照，新增/变化项先补备份；本阶段报告目录属于公共docs，保留原地。
2. 按逐段表准备公共规则、方向规则和新入口；统一解析公共路径与方向路径。
3. 复制并核验方向资料，修复可改写派生页和管理记录的实际链接；保留原件与历史语义。
4. 协调切换唯一生效入口、方向注册表、索引、日志和Obsidian/Git配置；原目录退役按迁移清单执行，回退副本保留。
5. 对照本基线验证数量、哈希、科学内容/状态及新增断链，记录例外和未测项，再进入阶段3隔离验收。

本阶段仅修改备份忽略规则、迁移方案/导航/操作日志并新增准备产物；研究源文件未搬动，未创建新研究方向或执行论文入库。
